
name: 'cac40_pipeline'
version: '1.0.0'
config-version: 2

profile: 'cac40_pipeline'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  cac40_pipeline:
    # couche Bronze : vues sur la table raw
    bronze:
      +materialized: view
      +schema: COMMON

    # couche Silver : nettoyage et typage
    silver:
      +materialized: view
      +schema: COMMON

    # couche Gold : KPIs business-ready
    gold:
      +materialized: table
      +schema: COMMON